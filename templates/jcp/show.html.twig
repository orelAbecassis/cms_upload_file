{% extends 'base.html.twig' %}

{% block title %}TEST{% endblock %}

{% block body %}
    <h1>TEST</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ t_e_s_t.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ t_e_s_t.Name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_t_e_s_t_index') }}">back to list</a>

    <a href="{{ path('app_t_e_s_t_edit', {'id': t_e_s_t.id}) }}">edit</a>

    {{ include('test/_delete_form.html.twig') }}
{% endblock %}
