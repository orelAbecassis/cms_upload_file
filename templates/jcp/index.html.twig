{% extends 'base.html.twig' %}

{% block title %}TEST index{% endblock %}

{% block body %}
    <h1>TEST index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for t_e_s_t in t_e_s_ts %}
            <tr>
                <td>{{ t_e_s_t.id }}</td>
                <td>{{ t_e_s_t.Name }}</td>
                <td>
                    <a href="{{ path('app_t_e_s_t_show', {'id': t_e_s_t.id}) }}">show</a>
                    <a href="{{ path('app_t_e_s_t_edit', {'id': t_e_s_t.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_t_e_s_t_new') }}">Create new</a>
{% endblock %}
